# Backend Environment Variables — UCAB Services
# Copy this file to .env and fill in your values

# ── PostgreSQL — Format 1: Individual variables (recommended for local dev) ───
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=yourpassword
DB_NAME=ucab_db

# ── PostgreSQL — Format 2: Single connection string (for cloud providers) ─────
# Supabase: copy the "URI" from Project Settings > Database
# Neon:     copy from Dashboard > Connection Details
# Railway:  copy from your service's Variables tab
# Render:   copy from your PostgreSQL service's Info tab
#
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/ucab_db?sslmode=require"

# ── Auth ──────────────────────────────────────────────────────────────────────
JWT_SECRET=ucab_secret_2026

# ── Server ────────────────────────────────────────────────────────────────────
PORT=5000
